# WisdomAI MVP — pinned runtime + dev dependencies.
#
# Pins are EXACT (==) on purpose: the app is developed on a personal laptop and
# deployed/continued from a different (company) machine + Cloud Run. Exact pins
# mean "works here" == "works there" == "works in the container" — no surprise
# minor-version drift between machines. Regenerate with:  pip freeze
# Verified working on Python 3.12 (container base) and 3.14 (local dev).

# ── Web framework / server ──
fastapi==0.139.0
starlette==1.3.1
uvicorn[standard]==0.50.2
python-multipart==0.0.32
pydantic==2.13.4

# ── Google AI (Vertex/Gemini via ADK) ──
google-adk==2.3.0
google-genai==2.10.0

# ── Google Cloud persistence (STORAGE=cloud backend) ──
google-cloud-firestore==2.28.0
google-cloud-storage==3.13.0

# ── Auth / secrets ──
bcrypt==5.0.0            # password hashing (used directly, not via passlib)
python-dotenv==1.2.2

# ── Document processing ──
openpyxl==3.1.5          # .xlsx text extraction on upload
fpdf2==2.8.7             # PDF export

# ── Test / dev (kept in the main file so a fresh clone can run the suite) ──
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1
